// Exercise 1: Functions II
//
// TODO 1: Write `void swapValues(int &a, int &b)` that swaps the values
//         of a and b, using reference parameters (no vector, no return -
//         the swap must be visible in the caller).
// TODO 2: Write an overloaded pair of functions called `maxOf`: one that
//         takes two ints and returns the larger, another that takes two
//         doubles and returns the larger.
// TODO 3: Write a recursive function `int sumUpTo(int n)` that returns
//         the sum of all integers from 1 to n (sumUpTo(5) = 15). Identify
//         your base case explicitly with a comment.
// TODO 4: In main, demonstrate all three with at least two calls each.
//
// Compile and run:
//   g++ exercise1.cpp -o exercise1
//   ./exercise1
#include <iostream>

int main() {
    // Your code here

    return 0;
}
