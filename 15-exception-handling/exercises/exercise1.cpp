// Exercise 1: Exception Handling
//
// TODO 1: Write a function double safeDivide(double a, double b) that
//         throws std::invalid_argument if b == 0, otherwise returns
//         a / b.
// TODO 2: Write a function int safeAt(std::vector<int> values, int index)
//         that uses .at(index) so an out-of-range index throws
//         std::out_of_range naturally (no manual check needed).
// TODO 3: In main, call safeDivide with a few inputs including a
//         divide-by-zero, wrapped in try/catch, printing either the
//         result or a clear error message.
// TODO 4: In main, call safeAt with a valid and an invalid index,
//         wrapped in try/catch, printing either the result or a clear
//         error message.
//
// Compile and run:
//   g++ exercise1.cpp -o exercise1
//   ./exercise1
#include <iostream>
#include <stdexcept>
#include <vector>

int main() {
    // Your code here

    return 0;
}
