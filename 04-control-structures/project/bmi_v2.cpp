// Module 4 Project, Part B: BMI Calculator v2 (with category detection)
// See project/README.md for requirements.
#include <iostream>

int main() {
    const double UNDERWEIGHT_MAX = 18.5;
    const double NORMAL_MAX = 25.0;
    const double OVERWEIGHT_MAX = 30.0;

    // TODO: read weight (kg) and height (m) from the user, as in Module 3.

    // TODO: compute bmi.

    // TODO: print the BMI, then use if/else if/else against the
    // constants above to print exactly one category:
    // Underweight / Normal / Overweight / Obese.

    return 0;
}
