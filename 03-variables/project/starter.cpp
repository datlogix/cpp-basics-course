// Module 3 Project: BMI Calculator
// See project/README.md for requirements.
#include <iostream>

int main() {
    const double UNDERWEIGHT_MAX = 18.5;
    const double NORMAL_MAX = 25.0;
    const double OVERWEIGHT_MAX = 30.0;

    // TODO: read weight (kg) from the user into a double.

    // TODO: read height (m) from the user into a double.

    // TODO: compute bmi = weight / (height * height).

    // TODO: print the BMI and the category boundaries.

    return 0;
}
